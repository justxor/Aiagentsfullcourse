# Зависимости курса. Принцип: чем меньше магии, тем понятнее, где ломается.
# Ничего из этого не является фреймворком для агентов - это осознанно.

# --- Ядро (нужно всегда) ---------------------------------------------
openai>=1.40            # клиент, совместимый с большинством провайдеров
pydantic>=2.7           # валидация схем инструментов и структурного вывода
jsonschema>=4.22        # проверка аргументов по JSON Schema
tiktoken>=0.7           # настоящий подсчёт токенов, не "символы/4"
python-dotenv>=1.0      # чтение .env
pyyaml>=6.0             # наборы задач и конфиги
rich>=13.7              # читаемый вывод трейсов в терминале

# --- Наблюдаемость (модуль 10) ---------------------------------------
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-http>=1.25

# --- RAG (модуль 05, опционально) ------------------------------------
numpy>=1.26             # достаточно до ~50k чанков, без внешних сервисов
rank-bm25>=0.2.2        # лексический поиск для гибрида
sqlite-vec>=0.1.1       # персистентное векторное хранилище на sqlite
# sentence-transformers>=3.0   # локальные эмбеддинги и cross-encoder реранк
# qdrant-client>=1.9           # если нужны фильтры на больших объёмах

# --- MCP (модуль 06, опционально) ------------------------------------
mcp>=1.1                # клиент и сервер Model Context Protocol

# --- Тесты и качество ------------------------------------------------
pytest>=8.2
pytest-xdist>=3.6       # параллельный прогон эвалов
ruff>=0.5
mypy>=1.10

# --- Что здесь намеренно отсутствует ---------------------------------
# Фреймворки для агентов: курс строит цикл, память и планирование руками,
#   чтобы вы контролировали контекст, стоимость и границы.
#   Честное сравнение "фреймворк или своё" - модуль 13, раздел 13.9.
# Векторные СУБД как сервисы: начинайте с numpy и sqlite-vec, переходите
#   на сервис, когда измерите, что упёрлись.
# Обёртки над логированием: структурные логи - это json.dumps в stderr.
